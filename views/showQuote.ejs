<h1>Here are the awesome quotes!</h1>
<% if(typeof(quotes) != "undefined") { %>
<% for(var x in quotes) { %>
    <h3>- "<%= quotes[x].quote %>"</h3>
    <h4>- <%= quotes[x].name %> at <%= quotes[x].createdAt%></h4>
<% } %>
<% } %>
<a href="/">Go back</a>